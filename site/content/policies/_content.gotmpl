{{ range $name, $p := hugo.Data.policies }}
  {{ $meta := $p.policy | default dict }}
  {{ $.AddPage (dict
       "path" $name
       "title" (printf "%s Policy" ($meta.name | default $name))
       "description" $meta.description
       "sitemap" (dict "priority" 0.5)
       "params" (dict "meta" $meta "file" (printf "%s.yml" $name))) }}
{{ end }}
